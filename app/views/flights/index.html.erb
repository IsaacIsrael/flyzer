<section class="flight-index">
  <% flight = Flight.first %>
  <div class="index-card d-flex w-100 flex-row justify-content-center align-items-center">
    <div>
      <p class="text-center"><i class="fas fa-plus"></i> Barato</p>
      <i class="fas fa-dollar-sign fa-3x p-4"></i>
    </div>
    <div class="card-body d-flex flex-column">
      <div>
        <h1><%= flight.departure_time.strftime("%H:%M") %> - <%= flight.arrival_time.strftime("%H:%M") %></h1>
      </div>
      <div>
        <p>Duração: <%= rand(30..200) %> min </p>
      </div>
      <div class="d-flex flex-row align-items-center justify-content-between">
        <h2>R$ <%= rand(1..9) %>.<%= rand(50..950) %>,00</h2>
        <p>Assentos Disponíveis: <%= flight.available_seats %></p>
      </div>
    </div>
  </div>
  <div class="index-card d-flex w-100 flex-row justify-content-center align-items-center">
    <div>
      <p class="text-center"><i class="fas fa-plus"></i> Rapido</p>
      <i class="fas fa-clock fa-3x p-4"></i>
    </div>
    <div class="card-body d-flex flex-column">
      <div>
        <h1><%= flight.departure_time.strftime("%H:%M") %> - <%= flight.arrival_time.strftime("%H:%M") %></h1>
      </div>
      <div>
        <p>Duração: <%= rand(30..200) %> min </p>
      </div>
      <div class="d-flex flex-row align-items-center justify-content-between">
        <h2>R$ <%= rand(1..9) %>.<%= rand(50..950) %>,00</h2>
        <p>Assentos Disponíveis: <%= flight.available_seats %></p>
      </div>
    </div>
  </div>
  <div class="index-card d-flex w-100 flex-row justify-content-center align-items-center">
    <div>
      <p class="text-center"><i class="fas fa-plus"></i> Conveniente</p>
      <i class="fas fa-clock fa-3x p-4"></i>
    </div>
    <div class="card-body d-flex flex-column">
      <div>
        <h1><%= flight.departure_time.strftime("%H:%M") %> - <%= flight.arrival_time.strftime("%H:%M") %></h1>
      </div>
      <div>
        <p>Duração: <%= rand(30..200) %> min </p>
      </div>
      <div class="d-flex flex-row align-items-center justify-content-between">
        <h2>R$ <%= rand(1..9) %>.<%= rand(50..950) %>,00</h2>
        <p>Assentos Disponíveis: <%= flight.available_seats %></p>
      </div>
    </div>
  </div>

  <h3 class="text-center">OUTROS VÔOS</h3>
  <% @flights.each do |flight| %>
  <div class="index-card d-flex w-100 ">
    <div class="card-body d-flex flex-column">
      <div>
        <h1><%= flight.departure_time.strftime("%H:%M") %> - <%= flight.arrival_time.strftime("%H:%M") %></h1>
      </div>
      <div>
        <p>Duração: <%= rand(30..200) %> min </p>
      </div>
      <div class="d-flex flex-row align-items-center justify-content-between">
        <h2>R$ <%= rand(1..9) %>.<%= rand(50..950) %>,00</h2>
        <p>Assentos Disponíveis: <%= flight.available_seats %></p>
      </div>
    </div>
  </div>
  <% end %>
</section>
